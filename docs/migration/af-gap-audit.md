# AFCrashpad ← Vesta migration: domain gap checklist

_Auto-generated audit of military / real-estate / branding items in the old AFCrashpad code (`pre-vesta-migration` tag) that are missing or genericized in the new Vesta-based app._

**Already restored:** Military calculators; Pipeline Military Base picker; Special Accommodations selector; Deal-sheet housing-allowance calculators with Apply-to-Deal; WorkspaceSwitcher brand text fallback already falls back to 'AFCrashpad CRM'

## 1. MISSING features / data (16)

- [ ] **[HIGH]** _(military/data-field)_ **militaryBase contact field — entire data model**
    - OLD: First-class contact field: validated in create/update/bulk Zod schemas; written on createContact, bulkCreateContacts, CSV import, and merge; in updateContact allowedKeys; returned by getContactsList and getContactDetail; used as {{militaryBase}} email merge token.
    - NEW: grep -c militaryBase src/app/contacts/actions.ts = 0 (confirmed). Schemas/createContact/import/merge all carry businessName but no militaryBase; allowedKeys dropped it and added generic dndUntil. Existing AFCrashpad Firestore docs still carry militaryBase but it is now orphaned/never read.
    - FIX: Re-add militaryBase to createContactSchema/updateContactSchema/bulkCreateContactsSchema, createContact, bulkCreateContacts, the import batch write (~actions.ts:1952), getContactsList, getContactDetail's contact object + both mergeContacts mergeFields arrays (~:1503,:2040), and the {{militaryBase}} email merge replacement.
- [ ] **[HIGH]** _(military/feature-page)_ **Military Bases admin manager (BasesManager) + military_bases CRUD server actions + periods/zipCode**
    - OLD: Full CRUD admin UI (search/add/inline-edit/delete) over military_bases with name, zipCode, and a periods subcollection (startDate/endDate/rate seasonal lodging rates). Mounted under settings 'Military Bases & Lodging'. Backed by getBases/createBase/updateBase/deleteBase that also loaded/managed the periods subcollection.
    - NEW: No src/app/settings/bases/ directory; find -iname '*BasesManager*' = nothing. Only a read-only getBaseNames() survives (src/app/pipeline/actions.ts:1315, marked @deprecated) reading just `name` — zipCode and the periods subcollection are never read. grep collection('periods') = nothing.
    - FIX: Recreate src/app/settings/bases/BasesManager.tsx + actions.ts (getBases/createBase/updateBase/deleteBase incl. periods subcollection + zipCode) on the tenantDb(workspaceId) pattern like StatusManager, and mount on a settings page. Without it the restored pipeline Base picker has no admin source.
- [ ] **[HIGH]** _(real-estate/feature-page)_ **Special Accommodations admin manager (SpecialAccommodationsManager) + special_accommodations CRUD actions**
    - OLD: Full CRUD admin UI for the special_accommodations collection (search/add/edit/delete options e.g. Pets/Spouse/Dependents), mounted in the System Properties grid; backed by getSpecialAccommodations/create/update/deleteSpecialAccommodation with name-uniqueness + auto-increment order + revalidatePath('/pipeline').
    - NEW: find -iname '*Accommodation*' = nothing; new src/app/settings/system-properties/ contains only StatusManager.tsx + a contact-status-only actions.ts. Only a read-only getSpecialAccommodations() survives in pipeline/actions.ts:1330 to drive the (restored) deal-sheet selector.
    - FIX: Recreate SpecialAccommodationsManager.tsx and port the four CRUD functions into system-properties/actions.ts using tenantDb + requireAdmin, mounted in settings.
- [ ] **[HIGH]** _(real-estate/automation-email)_ **autoAdvanceOpportunities() — auto-advance to Current Tenant on stay start (+ checkReferralConversion trigger)**
    - OLD: For deals in 'lease signed'/'move in scheduled', once stayStartDate <= today, moved them to 'current tenant' (writing stageHistory) and fired checkReferralConversion. Invoked daily by /api/cron/advance-opportunities.
    - NEW: grep autoAdvanceOpportunities src/ = 0 (confirmed). The cron route no longer imports/calls it; route is now a misnomer. Referral-conversion payouts tied to that transition also stop firing.
    - FIX: Restore the function and re-add it to the daily cron's per-workspace Promise.all (scoped via tenantDb like the other calls).
- [ ] **[HIGH]** _(real-estate/feature-page)_ **Guest check-in/out Automations settings tab (AutomationsTab) + Email SequenceManager UI (pre_checkin/post_checkout)**
    - OLD: settings/automations exposed guestRemindersEnabled, checkInReminderDays [1,3,7], checkOutReminderDays [1,3,7,30], guestCheckIn/OutTemplateId, plus auto-reply/auto-advance/stale toggles; SequenceManager built sequences with stay triggers 'Pre Check-In (7 days before)'/'Post Check-Out (1 day after)'.
    - NEW: settings/automations now contains ONLY actions.ts; AutomationsTab.tsx, SequenceManager.tsx, StageAutomations.tsx, page.tsx and the sequence/stage action files are missing. getAutomationSettings/updateAutomationSettings still return the guest-reminder fields but have NO UI consumer; reminders.ts still reads them, so they are backend-orphaned. /automations is the generic Vesta node-graph builder with no check-in/checkout/guest triggers.
    - FIX: Restore the automations settings UI (AutomationsTab + SequenceManager) or wire guestRemindersEnabled/checkIn/OutReminderDays/template IDs and pre_checkin/post_checkout sequence triggers into a settings page.
- [ ] **[HIGH]** _(military/automation-email)_ **Leads webhook: base/militaryBase mapping + on-base value calculation + stay-date writes + special_accommodations + reason_for_stay**
    - OLD: Inbound leads webhook had a `base` field with normalizeBaseName() (matched against lodgingData), wrote militaryBase on contact+opportunity; called calculateOnBaseLodging to set opportunityValue + 25% estimatedProfit; wrote stayStartDate/stayEndDate on both contact and opportunity; parsed special_accommodations into specialAccommodationId + specialAccommodationLabels; captured reason_for_stay->reasonForStay (+notes/auto-reply); accepted estimated_arrival_date/estimated_departure_date aliases and ?api_key= auth for Elementor.
    - NEW: route.ts schema field is now generic `location` (not base); militaryBase never written (grep confirms). opportunityValue = data.value || 0 with no lodging calc and no estimatedProfit; writes generic startDate/endDate (no stayStartDate/stayEndDate, none on contact); no special_accommodations/specialAccommodationId/specialAccommodationLabels; no reason_for_stay; estimated_* aliases + ?api_key= fallback removed. Downstream pipeline/reminders still read militaryBase/stayStartDate, so inbound leads break base picker, reminders, value, and stay-based priority.
    - FIX: Re-add `base`+normalizeBaseName->militaryBase, calculateOnBaseLodging value + 25% estimatedProfit, stayStartDate/stayEndDate writes (incl. contact), special_accommodations lookup, reason_for_stay capture, the estimated_* date aliases, and the ?api_key= query-param auth fallback.
- [ ] **[HIGH]** _(military/feature-page)_ **Military Base input in contact edit form (ContactDetailSheet)**
    - OLD: InlineField labeled 'Military Base' (placeholder 'e.g. Luke AFB') bound to editingContact.militaryBase, between Business Name and Contact Status.
    - NEW: The edit grid jumps from Business Name straight to Contact Status; no Military Base field exists anywhere in ContactDetailSheet.tsx.
    - FIX: Restore the Military Base InlineField with onBlurSave; depends on re-adding militaryBase to the data model + getContactDetail.
- [ ] **[HIGH]** _(military/feature-page)_ **Military Base in new/edit contact save (page.tsx handleSave/handleAddContactClick/handleCreateOpportunity)**
    - OLD: New-contact template seeded militaryBase:''; handleSave passed militaryBase to createContact/updateContact; handleCreateOpportunity passed base: selectedContact.militaryBase into createNewDeal so the deal pre-filled the base picker.
    - NEW: Template omits militaryBase; create/update payloads pass businessName + generic dndUntil but no militaryBase; handleCreateOpportunity no longer passes base.
    - FIX: Add militaryBase to the template, both payloads, and pass base from contact.militaryBase when creating an opportunity.
- [ ] **[MEDIUM]** _(military/data-field)_ **specialAccommodationLabels (free-text intake accommodations) capture + surface**
    - OLD: Free-text array (e.g. 'Traveling with Pet','Spouse','Dependents') captured from the leads webhook and surfaced by getPipelines and getContactDetail's opportunity projection.
    - NEW: grep specialAccommodationLabels src/ = 0 (confirmed). New getPipelines projection omits it; leads webhook has no special_accommodations handling. Distinct from the already-restored specialAccommodationId (configured options) — this carried raw intake labels.
    - FIX: Re-add webhook capture and the getPipelines + getContactDetail opportunity projection of specialAccommodationLabels.
- [ ] **[MEDIUM]** _(real-estate/automation-email)_ **moveToLeaseSigned() action + gated 'Move to Lease Signed' button**
    - OLD: Finds the 'Lease Signed' stage in the deal's pipeline, moves the opp there, writes stageHistory, runs executeStageAutomations. Surfaced as a green button in DealDetailSheet once lease+tc+payment docs are all checked.
    - NEW: grep moveToLeaseSigned src/ = 0 (confirmed); not imported in DealDetailSheet.tsx. The new Docs tab renders the checklist but has no completion/advance button.
    - FIX: Re-add moveToLeaseSigned() (resolve the stage by name in the deal's own pipeline) and the docs-complete gated button in DealDetailSheet.
- [ ] **[MEDIUM]** _(military/collection)_ **Military Base import mapping + auto-map heuristic (ImportMappingDialog) and bulk/import batch write**
    - OLD: Import column mapping offered a 'Military Base' target and auto-mapped headers containing base/military/installation; bulkCreateContacts + CSV import batch write set militaryBase on each created doc.
    - NEW: ImportMappingDialog.tsx keeps businessName but the militaryBase mapping option and auto-map heuristic are gone; import batch write (~actions.ts:1952) and bulkCreateContactsSchema no longer carry militaryBase.
    - FIX: Re-add the militaryBase mapping option + base/military/installation auto-map heuristic, plus militaryBase in bulkCreateContactsSchema and the import batch write.
- [ ] **[MEDIUM]** _(military/reporting)_ **Military Base column in contacts CSV export**
    - OLD: Contacts CSV export included a 'Military Base': c.militaryBase || '' column.
    - NEW: Export object (page.tsx:1242) goes from Status straight to 'Business Name'; the Military Base column is gone.
    - FIX: Add the Military Base column back to the exported contact rows.
- [ ] **[MEDIUM]** _(military/feature-page)_ **Military Base in contact merge field picker + server mergeFields**
    - OLD: Merge dialog field list and the server mergeContacts mergeFields arrays included militaryBase so merging preserved/chose the base.
    - NEW: UI field list (page.tsx:1864-1869) and both server mergeFields arrays (actions.ts:1503,2040) omit militaryBase.
    - FIX: Add militaryBase back to the UI merge picker and both server-side mergeFields arrays.
- [ ] **[MEDIUM]** _(military/collection)_ **Hardcoded military_bases + default special_accommodations seeding**
    - OLD: scripts/seed-bases.mjs + seed-firebase.ts seeded ~70 AF bases with zip codes and default special_accommodations ['Traveling with Pet','Spouse','Dependents'].
    - NEW: The curated base names/zips survive only as the static baseToZip/lodgingData constants in src/lib/calculators/on-base.ts; nothing seeds the military_bases or special_accommodations Firestore collections, and there is no admin UI to add them, so a fresh/migrated workspace's pickers start empty.
    - FIX: Provide a seed/import path (or a BasesManager 'import from FY list' button) that pushes on-base.ts's bases into military_bases and seeds default special_accommodations for the tenant.
- [ ] **[LOW]** _(military/reporting)_ **Military Base CSV export column on pipeline deals**
    - OLD: Pipeline CSV export rows included 'Military Base': d.militaryBase || '' (alongside Stay Start/Stay End).
    - NEW: Pipeline export rows (page.tsx:1097-1106) keep Stay Start/Stay End but the Military Base column was removed.
    - FIX: Re-add the 'Military Base' column to the pipeline export mapping.
- [ ] **[LOW]** _(military/data-field)_ **militaryBase in DuplicateContact type + duplicate-detection projection**
    - OLD: DuplicateContact type included militaryBase and the duplicate-detection projection read it.
    - NEW: types.ts DuplicateContact has no militaryBase; the new duplicate query projection (actions.ts:1377) selects businessName but not militaryBase.
    - FIX: Add militaryBase back to the DuplicateContact type and the duplicate-detection projection for parity.

## 2. GENERICIZED labels / wording (34)

- [ ] **[HIGH]** App metadata title (browser tab / PWA): `AFCrashpad CRM` → `Vesta CRM`  — src/app/layout.tsx:12 title='Vesta CRM', :18 appleWebApp.title='Vesta CRM' (confirmed via grep).
- [ ] **[HIGH]** PWA manifest name/short_name/description: `AFCrashpad CRM / AFCrashpad / Air Force Crashpad CRM` → `Vesta CRM / Vesta / Modern sales CRM`  — public/manifest.json:2-4 name 'Vesta CRM', short_name 'Vesta', description 'Modern sales CRM' (confirmed).
- [ ] **[HIGH]** Deal expense section heading: `Host Expenses` → `Cost of Sale`  — DealDetailSheet.tsx:1379 h3 'Cost of Sale'.
- [ ] **[HIGH]** Expense field label (monthlyRent): `Monthly Host Rent ($)` → `Recurring Cost / mo ($)`  — DealDetailSheet.tsx:1388,1394 bound to same expensesData.monthlyRent.
- [ ] **[HIGH]** Expense field label (cleaningFee): `Cleaning Fee ($)` → `Service Fee ($)`  — DealDetailSheet.tsx:1400,1406 bound to expensesData.cleaningFee.
- [ ] **[HIGH]** Expense field label (petFee): `Pet Fee ($)` → `Other Variable Cost ($)`  — DealDetailSheet.tsx:1412,1418 bound to expensesData.petFee.
- [ ] **[HIGH]** Expense field label (nonrefundableDeposit): `Non-refundable Deposit ($)` → `Fixed Setup Cost ($)`  — DealDetailSheet.tsx:1424,1430 bound to expensesData.nonrefundableDeposit.
- [ ] **[HIGH]** Deal sheet stay-details section heading: `Stay Details` → `Schedule`  — DealDetailSheet.tsx:724 h3 'Schedule' (comment 'Period Info').
- [ ] **[HIGH]** Deal date field labels (startDate/endDate): `Check-in Date / Check-out Date` → `Start Date / End Date`  — DealDetailSheet.tsx:728,737 on the same startDate/endDate fields.
- [ ] **[HIGH]** Default contact status taxonomy (fallback set + edit <select>): `Active Stay / Lead / Forms Pending / Booked` → `Lead / Customer / Past Customer`  — page.tsx:1282,1383 fallbacks; ContactDetailSheet.tsx:716-719 <select>. Color map (page.tsx:1156-1159) still recognizes Active Stay/Forms Pending/Booked, so it half-expects them.
- [ ] **[HIGH]** Dashboard booked/revenue stage set dropped 'Lease Signed': `includes 'Lease Signed' (revenue recognized at Lease Signed)` → `['Closed Won','Won','Booked','Signed','Closed'] (no Lease Signed)`  — dashboard/actions.ts:655 + pipeline/actions.ts:973 dropped 'Lease Signed'; deals at Lease Signed no longer auto-record commission and KPIs/forecast undercount.
- [ ] **[HIGH]** Dashboard 'Active Tenants' KPI label + drill-down link: `'Active Tenants' -> /contacts?status=Active+Stay` → `'Active customers' -> /contacts?status=Customer`  — GridDashboard.tsx:269-279 + page.tsx:88; value still counts status==='Active Stay' (actions.ts:171), so the count is right but the drill-down filters to Customer — a broken/mismatched link.
- [ ] **[HIGH]** Default seeded pipeline (13-stage Traveler Placement): `'Traveler Placement' — New Lead, Contacted, Finding Properties, Selecting Property, Lease Sent, Lease Signed, Move-in Scheduled, Current Tenant, Move-out Scheduled, Review/Referral, Closed Won, Closed Lost, Archive` → `'Sales Pipeline' (7 generic stages); the crashpad lifecycle only survives as an opt-in 'Rental Pipeline' snapshot with DIFFERENT 8-stage names`  — src/lib/snapshots/generic.ts:16-25 is the default; real-estate-rental.ts:14-25 renames stages (no Finding Properties/Selecting Property/Lease Sent/Move-out Scheduled/Review/Referral/Archive; 'Move-in'->'Move In'), breaking stage-name-coupled logic. New workspaces get generic stages.
- [ ] **[HIGH]** Blog AI generation prompt audience/context: `Tone 'written for military service members'; afcrashpad.com crashpad housing for Air Force personnel during TDY/PCS` → `Generic 'Authority Architect' SEO prompt, 'Professional but approachable', no AF/TDY/PCS context`  — src/app/api/blog/generate/route.ts:45-72; AI blog content no longer defaults to the AF crash-pad audience.
- [ ] **[HIGH]** dealsByBase computation (base-distribution chart data) + 'Inquiry Tracker' widget: `Computed per-base deal counts (top-8 dealsByBase); widget 'Inquiry Tracker — Deals per Military Base' in the default layout` → `dealsByBase: [] hardcoded empty; widget 'Inquiry tracker / Deals by location', and NOT in any default DASHBOARD_TEMPLATES layout (gallery-only)`  — dashboard/actions.ts:323 dealsByBase:[]; militaryBase dropped from opp projection (:114-128) and contactBaseMap deleted; widget-registry.tsx:181-190 + GridDashboard.tsx:416-432 genericized; widget absent from templates (widget-registry.tsx:322-427).
- [ ] **[MEDIUM]** Required Agreements checklist labels: `Homeowner Lease / AF Crashpad Terms & Conditions` → `Master Agreement / Terms & Conditions`  — ContactDetailSheet.tsx:927-929 map to the same preserved fields homeownerLeaseSigned/termsConditionsSigned; only display labels genericized.
- [ ] **[MEDIUM]** Conversion analytics terminal-win set dropped 'Current Tenant': `CLOSED_WON_NAMES includes 'Current Tenant'` → `['Closed Won','Won','Booked','Signed','Closed'] (no Current Tenant)`  — pipeline/analytics/actions.ts:31; conversion funnel no longer counts reaching Current Tenant as a win, distorting stage-conversion rates.
- [ ] **[MEDIUM]** Forecasting closedNames dropped 'Current Tenant': `closedNames includes 'Current Tenant'` → `['Closed Won','Won','Booked','Signed','Closed','Closed Lost','Lost','Abandoned'] (no Current Tenant)`  — finance/forecasting/actions.ts:75; active-tenant deals are mis-weighted as open and projected into future months (stay-date close-date driving is preserved).
- [ ] **[MEDIUM]** Commission record 'base' at write time: `base: oppData.militaryBase || null` → `base: null (hardcoded)`  — commissions/actions.ts:140; CommissionEntry.base + the UI ('{entry.base || "No base"}') are intact, so new commissions always show 'No base'.
- [ ] **[MEDIUM]** Stay/guest reminder {{base}} variable + base in notifications/sequences (reminders.ts): `reads data.militaryBase into templateVars.base; substitutes {{base}}; passes base to pre_checkin/post_checkout sequences; notification msg includes ' — {base}'` → `no base templateVar; {{base}} resolves to 'your location'; sequence calls + notification msg omit base`  — reminders.ts:80-85,95,152,105; check-in/out emails and notifications no longer show the base.
- [ ] **[MEDIUM]** 'Show Base Location' card-field toggle (showBase): `'Show Base Location' toggle threading real showBase into Kanban/List base badge/column` → `toggle removed; showBase={false} hardcoded to both views`  — pipeline/page.tsx:1373,1498; KanbanView/ListView still support showBase (render code intact) but it can never be enabled.
- [ ] **[MEDIUM]** Profit-summary line: `Prorated Rent` → `Prorated Recurring Cost`  — DealDetailSheet.tsx:1460 (logic identical — calculateProratedRent on monthlyRent).
- [ ] **[MEDIUM]** Dashboard widget title (kpi-active-customers): `Active Tenants / Active Stays` → `Active customers`  — widget-registry.tsx:90-91 surfaces activeStayCount under a generic title.
- [ ] **[LOW]** getContactDetail opportunity projection dropped base + specialAccommodation: `included militaryBase, specialAccommodationId, specialAccommodationLabels` → `keeps reasonForStay/stay dates but omits militaryBase/specialAccommodationId/specialAccommodationLabels`  — contacts/actions.ts:953-977; restored on the pipeline page but this contacts-side read path still strips them, so opps inside the Contact sheet lack base/accommodation.
- [ ] **[LOW]** Contact 'Stay Start / Stay End' date labels + helper text: `'Stay Start/End (optional)' + 'Stay dates ... pre-fill the deal form'` → `'Period Start/End (optional)' + 'Period dates pre-fill new opportunities — engagement/project/service durations'`  — ContactDetailSheet.tsx:728,734,743 (field names stayStartDate/stayEndDate preserved).
- [ ] **[LOW]** Pipeline card toggles + ListView headers (check-in/out): `'Show Check-in Date'/'Show Check-out Date'; List headers 'Check-in'/'Check-out'` → `'Show Start Date'/'Show End Date'; List headers 'Start'/'End'`  — pipeline/page.tsx:1030,1032; ListView.tsx:118-119.
- [ ] **[LOW]** Required-docs default label (DealDetailSheet): `Homeowner Lease (+ example copy crash-pad specific)` → `Signed Contract (empty-state suggests generic 'NDA, Statement of Work, W-9')`  — settings/required-docs/actions.ts:24-28 DEFAULT_DOCS 'lease' label; DealDetailSheet.tsx:1304 empty-state copy. Keys lease/tc/payment preserved. Note: getPipelines (actions.ts:277-281) still hardcodes only lease/tc/payment so custom doc keys aren't loaded onto deals.
- [ ] **[LOW]** Deal financial section heading: `Financial Calculation` → `Deal Value`  — DealDetailSheet.tsx:959 (calculator dialog itself restored; only heading wording changed).
- [ ] **[LOW]** Referral status wording 'Active Tenant': `'Active Tenant' label / 'convert to active tenant' / 'Payout unlocks at Active Tenant'` → `'Active' / 'convert to active' / 'Payout unlocks at Active' / 'becomes an active customer'`  — ReferralTracker.tsx:41,273,324,687,522; underlying status key 'active_tenant' + logic preserved.
- [ ] **[LOW]** Email sequence {{base}} not populated from webhook (location vs base): `webhook new_contact sequence extraVars { base: data.base }` → `extraVars { location: data.location }; substituteTemplate only maps {{base}}, so AF templates render 'your location'`  — webhooks/leads/route.ts:204-208 + email-sequences.ts substituteTemplate.
- [ ] **[LOW]** Auto-reply email {{base}} + inquiry summary labels: `{{base}} substitution; summary rows 'Base:', 'Arrival:', 'Departure:', 'Reason for Stay:'` → `{{location}} substitution; summary 'Location:', 'Start Date:', 'End Date:' (no Base/Reason)`  — webhooks/leads/route.ts:376-388; existing {{base}}-authored templates won't substitute.
- [ ] **[LOW]** HARO settings default website: `https://afcrashpad.com` → `'' (empty)`  — marketing/haro/types.ts:120,132.
- [ ] **[LOW]** Blog SEO schema/publisher defaults: `publisher 'AFCrashpad', url afcrashpad.com, author 'AFCrashpad Team'` → `publisher branding.companyName || 'Your Company', author 'Content Team', siteUrl from branding`  — marketing/blog/SchemaGenerator.tsx:39,48,52-59 — acceptable IF branding.companyName/websiteUrl are set to AFCrashpad; verify the branding doc is seeded.
- [ ] **[LOW]** Blog editor preview URL placeholder: `afcrashpad.com/blog/{slug}` → `yoursite.com/blog/{slug}`  — marketing/blog/ArticleEditor.tsx:365,523.

## 3. VESTA branding leftovers (22)

- [ ] App + PWA title/manifest — src/app/layout.tsx:12,18 'Vesta CRM'; public/manifest.json:2-4 'Vesta CRM'/'Vesta'/'Modern sales CRM' (confirmed). Browser tab + PWA home-screen install name. severity:high
- [ ] Notification email footer brand — src/lib/notification-dispatch.ts:71 'Vesta CRM • You can manage email preferences in Settings' (confirmed) — sent in EVERY notification email. severity:high
- [ ] PDF export footer brand — src/lib/export-pdf.ts:152 'Exported from Vesta CRM — {date}' (confirmed) — stamped on every dashboard/finance PDF export (dashboard/page.tsx:587 calls exportToPDF). severity:high
- [ ] Referral program email footer — src/app/dashboard/referrals/actions.ts:423 'Vesta CRM • Referral Program' in referral-payout emails. severity:high
- [ ] Public lead-form footer + wrapper class — src/components/forms/FormRenderer.tsx:390 'Powered by Vesta CRM' on public embedded lead-capture forms; wrapper class vesta-form-{id} (:244,:247). Public-facing. severity:high
- [ ] Public marketing footer/nav — src/components/marketing/PublicFooter.tsx:11,32 'Vesta CRM' wordmark + '© {year} Vesta CRM. All rights reserved.'; PublicNav also Vesta-branded. These pages are new (not in old app). severity:high
- [ ] Per-page metadata titles (admin/support/suspended/public) — 'Admin · Vesta' (admin/layout.tsx:7), 'Support session · Vesta' (support/page.tsx:8), 'Workspace suspended · Vesta' (suspended/page.tsx:8), '... | Vesta CRM' on all (public) pages (terms/privacy/status/changelog/help). Browser tab titles. severity:medium
- [ ] MobileTopNav brand fallback — src/components/layout/MobileTopNav.tsx:39 pageTitle fallback companyName || 'Vesta CRM' — inconsistent with Sidebar/WorkspaceSwitcher which fall back to 'AFCrashpad CRM'. severity:medium
- [ ] Setup checklist heading — src/components/layout/SetupChecklistButton.tsx:155 'Get started with Vesta'. User-visible. severity:medium
- [ ] AI assistant identity — AssistantWidget.tsx:143 header subtitle 'Vesta CRM'; system-prompt.ts:8 'You are an in-app assistant for Vesta CRM'. Assistant tells users it is Vesta. severity:medium
- [ ] support@vestacrm.com support address — Hardcoded in terms/page.tsx:89, privacy/page.tsx:88, help/page.tsx:321, settings/workspace/support-access/SupportAccessClient.tsx:255. Wrong support address shown to users. severity:medium
- [ ] robots.ts / sitemap.ts default base URL — src/app/robots.ts:4 + sitemap.ts:9 baseUrl fallback 'https://vesta-crm.com'. If NEXT_PUBLIC_APP_URL unset, emits vesta-crm.com URLs (should be afcrashpad.com). severity:medium
- [ ] Outbound webhook X-Vesta-* headers + User-Agent — src/lib/webhooks/dispatcher.ts:111-115 sends X-Vesta-Event/Delivery-Id/Signature/Timestamp + UA 'Vesta-Webhooks/1.0' to customer endpoints; docs reference X-Vesta-Signature (WebhooksManager.tsx:49,175; help/page.tsx:245). Vesta brand leaks over the wire to integrators. severity:medium
- [ ] Default brand icon (Hexagon) — WorkspaceSwitcher renders a violet Lucide <Hexagon/> when no custom logoUrl (src/components/layout/WorkspaceSwitcher.tsx ~33-38); MobileTopNav also uses <Hexagon/> (:48). Vesta's logo motif. severity:low
- [ ] favicon / apple-touch-icon assets — No public/favicon.ico or apple-touch-icon.png; only src/app/favicon.ico + icon.png (Vesta-era) and public/icons/icon-192/512.png. Verify these are the AFCrashpad logo, not Vesta's hexagon; layout still references /icons/icon-192.png. severity:medium
- [ ] Feedback / suspended operator email + subject — growwithvesta@gmail.com recipient (feedback/actions.ts:92) + mailto (suspended/page.tsx:68); subject prefix '[Vesta {label}]' (feedback/actions.ts:113). Suspended page tells AF users to email Vesta. severity:low
- [ ] Feedback dialog/welcome copy + localStorage key — 'What would make Vesta better for you?' (FeedbackDialog.tsx:122); 'Help shape Vesta' (FeedbackWelcomeModal.tsx:67); key vesta:feedback-intro-seen. severity:low
- [ ] Changelog content — 'Vesta CRM Launch' (Changelog.tsx:22); 'What's new in Vesta' (changelog/page.tsx:24). Public changelog Vesta-branded. severity:low
- [ ] Billing plan tagline — 'Try Vesta with no time limit' (src/lib/billing/plans.ts:111). User-visible if billing UI surfaces taglines. severity:low
- [ ] Internal vesta salts/cookies/secrets — TRACKING_SALT || 'vesta' (email-tracking.ts:31); 'vesta-dev-fallback-*' (support/grants.ts:42, email/unsubscribe.ts:28); support cookie 'vesta-support-session' (support/session.ts:5). Non-visible but worth neutralizing. severity:low
- [ ] Dead 'Bases'/'accommodations' nav pointers — SettingsSearch.tsx:10 keywords 'bases'/'accommodations' point to 'workspace' tab and Breadcrumbs.tsx:24 maps bases:'Bases', but no bases/accommodations route exists in the new app — dead pointers until BasesManager/SpecialAccommodationsManager are restored. severity:low
- [ ] Default outbound from-email no longer defaults to AFCrashpad — src/lib/email.ts:38-40,85-87 removed the 'AFCrashpad CRM <support@afcrashpad.com>' fallback and now throws if RESEND_FROM_EMAIL is unset. Verify the env var is set for this workspace, else outbound email loses AF sender identity (or hard-fails). severity:medium

---
_Summary:_ Audit of the Vesta→AFCrashpad re-duplication. The calculators/Tools hub, the pipeline-side Military Base picker (getBaseNames), the specialAccommodationId selector, and the deal-sheet housing-allowance calculator with apply-to-deal wiring are confirmed already restored. Three large gap clusters remain: (1) MISSING — the entire `militaryBase` contact data model (schemas, create/bulk/import/merge writes, getContactsList/getContactDetail reads, edit field, list subtitle, CSV column, merge picker, import mapping), the BasesManager + military_bases CRUD/periods/zipCode admin, the SpecialAccommodationsManager + CRUD, specialAccommodationLabels free-text capture, autoAdvanceOpportunities + moveToLeaseSigned lifecycle automations, the guest check-in/out Automations + SequenceManager UI, and webhook capture of base/stay-dates/accommodations/reason/value. (2) GENERICIZED — AF lodging vocabulary swapped for generic CRM terms across the deal sheet (Stay Details→Schedule, Check-in/out→Start/End, Host Expenses→Cost of Sale and its 4 cost labels, Homeowner Lease→Signed Contract), contact statuses (Active Stay/Forms Pending/Booked→Lead/Customer/Past Customer), dashboard/forecasting/analytics stage sets dropping Lease Signed/Current Tenant (causing commission + KPI undercount), Active Tenants KPI→Active customers with a broken status=Customer drill-down, reminders/webhook {{base}}→{{location}}, blog AI prompt losing the AF/TDY/PCS context, and dealsByBase hardcoded empty. (3) VESTA LEFTOVERS — user-visible "Vesta CRM" branding in the app/PWA title+manifest, notification + PDF + referral email footers, public lead-form/marketing/changelog/assistant surfaces, support@vestacrm.com, vesta-crm.com sitemap/robots fallback, X-Vesta-* webhook headers, and the default Hexagon logo. Severity is dominated by the militaryBase data-model loss (orphans existing Firestore data), the dropped revenue stages, and the high-traffic branding leaks (tab title, every notification/PDF/referral email, public forms).
