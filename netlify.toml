[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  NODE_OPTIONS = "--max-old-space-size=4096"
  # NEXT_PUBLIC_* vars are baked into client bundle by design; exclude build output from secrets scan
  SECRETS_SCAN_OMIT_PATHS = ".netlify/.next/*"
